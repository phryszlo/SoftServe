import React from 'react';
import { useNavigate } from 'react-router-dom';

const NewThing = ({ fields, title }) => {

  const navigate = useNavigate();

  return (
    <div>NewThing</div>
  )
}

export default NewThing;